import { {{pascalCase name}}Model, Edit{{pascalCase name}}Model } from '@usecases'

export interface Edit{{pascalCase name}}RepositoryProtocol {
  edit: (
    id: string,
    params: Edit{{pascalCase name}}RepositoryProtocol.Params,
  ) => Promise<Edit{{pascalCase name}}RepositoryProtocol.Result>
}

export namespace Edit{{pascalCase name}}RepositoryProtocol {
  export type Result = {{pascalCase name}}Model
  export type Params = Edit{{pascalCase name}}Model.Params
}
