import { {{pascalCase name}}Model, Edit{{pascalCase name}}Model } from '@usecases'

export interface Edit{{pascalCase name}}Protocol {
  edit: (
    id: string,
    params: Edit{{pascalCase name}}Model.Params,
  ) => Promise<Edit{{pascalCase name}}Protocol.Result>
}

export namespace Edit{{pascalCase name}}Protocol {
  export type Result = {{pascalCase name}}Model
}
