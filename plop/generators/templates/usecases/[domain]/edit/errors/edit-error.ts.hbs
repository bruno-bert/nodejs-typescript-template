export class Edit{{pascalCase name}}Error extends Error {
  constructor(error: string) {
    super(`Error on editing {{kebabCase name}}: ${error}`)
    this.name = 'Edit{{pascalCase name}}Error'
  }
}

export class Edit{{pascalCase name}}NotFoundError extends Edit{{pascalCase name}}Error {
  constructor(error: string) {
    super(`Error on editing {{kebabCase name}}: ${error}`)
    this.name = 'Edit{{pascalCase name}}Error'
  }
}

export class DatabaseEdit{{pascalCase name}}Error extends Edit{{pascalCase name}}Error {
  constructor(error: string) {
    super(`Error on editing {{kebabCase name}} in database: ${error}`)
    this.name = 'DatabaseEdit{{pascalCase name}}Error'
  }
}

export class DatabaseEdit{{pascalCase name}}NotFoundError extends Edit{{pascalCase name}}NotFoundError {
  constructor(error: string) {
    super(`Error on editing {{kebabCase name}} in database: ${error}`)
    this.name = 'DatabaseEdit{{pascalCase name}}Error'
  }
}
