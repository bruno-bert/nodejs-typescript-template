export class Load{{pascalCase name}}Error extends Error {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}}: ${error}`)
    this.name = 'Load{{pascalCase name}}Error'
  }
}

export class DatabaseLoad{{pascalCase name}}Error extends Error {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} in database: ${error}`)
    this.name = 'DatabaseLoad{{pascalCase name}}Error'
  }
}
