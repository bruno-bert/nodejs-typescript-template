import { {{pascalCase name}}Model } from '@usecases'

export interface Load{{pascalCase name}}RepositoryProtocol {
  loadAll: () => Promise<Load{{pascalCase name}}RepositoryProtocol.Result>
}

export namespace Load{{pascalCase name}}RepositoryProtocol {
  export type Result = {{pascalCase name}}Model[]
}
