export class Delete{{pascalCase name}}Error extends Error {
  constructor(error: string) {
    super(`Error on deleting {{kebabCase name}}: ${error}`)
    this.name = 'Delete{{pascalCase name}}Error'
  }
}

export class Delete{{pascalCase name}}NotFoundError extends Error {
  constructor(error: string) {
    super(`Error on deleting {{kebabCase name}}: ${error}`)
    this.name = 'Delete{{pascalCase name}}NotFoundError'
  }
}

export class DatabaseDelete{{pascalCase name}}Error extends Delete{{pascalCase name}}Error {
  constructor(error: string) {
    super(`Error on deleting {{kebabCase name}} in repository: ${error}`)
    this.name = 'Delete{{pascalCase name}}NotFoundError'
  }
}
export class DatabaseDelete{{pascalCase name}}NotFoundError extends Delete{{pascalCase name}}NotFoundError {
  constructor(error: string) {
    super(`Error on deleting {{kebabCase name}} in repository: ${error}`)
    this.name = 'Delete{{pascalCase name}}NotFoundError'
  }
}
