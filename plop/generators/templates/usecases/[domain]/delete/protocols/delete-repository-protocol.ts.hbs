import { Delete{{pascalCase name}}Model } from '@usecases'

export interface Delete{{pascalCase name}}RepositoryProtocol {
  delete: (
    params: Delete{{pascalCase name}}RepositoryProtocol.Params,
  ) => Promise<Delete{{pascalCase name}}RepositoryProtocol.Result>
}

export namespace Delete{{pascalCase name}}RepositoryProtocol {
  export type Result = { success: boolean; count: number }
  export type Params = Delete{{pascalCase name}}Model.Params
}
