import { Delete{{pascalCase name}}Model, Delete{{pascalCase name}}RepositoryProtocol } from '@usecases'

export interface Delete{{pascalCase name}}Protocol {
  map: (
    params: Delete{{pascalCase name}}Model.Params,
  ) => Promise<Delete{{pascalCase name}}RepositoryProtocol.Params>
  delete: (params: Delete{{pascalCase name}}Model.Params) => Promise<Delete{{pascalCase name}}Protocol.Result>
}

export namespace Delete{{pascalCase name}}Protocol {
  export type Result = { success: boolean; count: number }
}
