export class Load{{pascalCase name}}PagingError extends Error {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} paging: ${error}`)
    this.name = 'Load{{pascalCase name}}PagingError'
  }
}

export class DatabaseLoad{{pascalCase name}}PagingError extends Error {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} paging in database: ${error}`)
    this.name = 'DatabaseLoad{{pascalCase name}}PagingError'
  }
}
