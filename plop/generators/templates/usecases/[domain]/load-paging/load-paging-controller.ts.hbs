import { Controller, HttpResponse } from '@presentation/protocols'
import { noContent, ok } from '@presentation/helpers'
import { Load{{pascalCase name}}PagingProtocol } from './protocols'
import { errorToHttpResponse } from '@utils/error-handler'

export class Load{{pascalCase name}}PagingController implements Controller {
  constructor(private readonly Load{{pascalCase name}}Paging: Load{{pascalCase name}}PagingProtocol) {}

  async handle(
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    request: Load{{pascalCase name}}PagingController.Request,
  ): Promise<HttpResponse> {
    try {
      const data = await this.Load{{pascalCase name}}Paging.loadPaging()
      return data ? ok(data) : noContent()
    } catch (error: any) {
      return errorToHttpResponse(error)
    }
  }
}

export namespace Load{{pascalCase name}}PagingController {
  export type Request = Record<string, never>
}
