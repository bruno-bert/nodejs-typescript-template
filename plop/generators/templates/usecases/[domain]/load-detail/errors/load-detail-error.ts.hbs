export class Load{{pascalCase name}}DetailError extends Error {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} detail: ${error}`)
    this.name = 'Load{{pascalCase name}}DetailError'
  }
}

export class Load{{pascalCase name}}DetailNotFoundError extends Error {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} detail: ${error}`)
    this.name = 'Load{{pascalCase name}}DetailNotFoundError'
  }
}

export class DatabaseLoad{{pascalCase name}}DetailError extends Load{{pascalCase name}}DetailError {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} detail in database: ${error}`)
    this.name = 'DatabaseLoad{{pascalCase name}}DetailError'
  }
}

export class DatabaseLoad{{pascalCase name}}DetailNotFoundError extends Load{{pascalCase name}}DetailNotFoundError {
  constructor(error: string) {
    super(`Error on loading {{kebabCase name}} detail in database: ${error}`)
    this.name = 'DatabaseLoad{{pascalCase name}}DetailNotFoundError'
  }
}
