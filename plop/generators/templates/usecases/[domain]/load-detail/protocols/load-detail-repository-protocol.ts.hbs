import { {{pascalCase name}}Model } from '@usecases'

export interface Load{{pascalCase name}}DetailRepositoryProtocol {
  loadById: (id: string) => Promise<Load{{pascalCase name}}DetailRepositoryProtocol.Result>
}

export namespace Load{{pascalCase name}}DetailRepositoryProtocol {
  export type Params = { id: string }
  export type Result = {{pascalCase name}}Model
}
