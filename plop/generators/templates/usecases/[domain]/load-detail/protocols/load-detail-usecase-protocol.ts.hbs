import { {{pascalCase name}}Model } from '@usecases'

export interface Load{{pascalCase name}}DetailProtocol {
  load: (id: string) => Promise<Load{{pascalCase name}}DetailProtocol.Result>
}

export namespace Load{{pascalCase name}}DetailProtocol {
  export type Result = {{pascalCase name}}Model
}
