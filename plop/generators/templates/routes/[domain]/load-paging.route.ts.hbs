import { adaptRoute } from '@main/adapters'
import { makeLoad{{pascalCase name}}PagingFactory } from '@main/factories'
import { Router } from 'express'

export default (router: Router): void => {
  router.get(
    '/{{kebabCase name}}/paging',
    adaptRoute(makeLoad{{pascalCase name}}PagingFactory().makeController()),
  )
}
